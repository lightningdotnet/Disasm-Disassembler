#pragma once

#include <cstdint>
#include <cstddef>

namespace core {

using Addr    = uint64_t;   // virtual address
using RVA     = uint32_t;   // relative virtual address
using FileOff = uint32_t;   // file offset

} // namespace core
